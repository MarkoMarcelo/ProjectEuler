
#include <iostream>
using namespace std;

int main(){
    int suma=0;
    int n=1000;
    for(int i=0 ; i<n ; i++) 
    {
        if(5*i<n)
            suma+=5*i;
        if(3*i<n && (3*i) %5 != 0)
            suma += 3*i;
    }
    cout<<"Suma: "<<suma<<endl;
    return 0;
 }
